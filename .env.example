# Copie para .env e preencha os valores.
# No Render, configure essas variáveis no dashboard (Environment).

DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME
DATABASE_SSL=true
JWT_SECRET=troque-por-um-segredo-forte
PORT=3000
